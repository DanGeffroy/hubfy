export class User {
  local            : {
      email        : String,
      password     : String,
  };
  facebook         : {
      id           : String,
      token        : String,
      email        : String,
      name         : String
  };
  twitter          : {
      id           : String,
      token        : String,
      displayName  : String,
      username     : String
  };
  google           : {
      id           : String,
      token        : String,
      email        : String,
      name         : String
  };
    simpletodo     : {
      col:  Number,
      row : Number,
      sizex : Number,
      sizey : Number,
      width : Number,
      height : Number
    };
    twitchchat     : {
      col:  Number,
      row : Number,
      sizex : Number,
      sizey : Number,
      width : Number,
      height : Number
    };
    twitchplayer     : {
      col:  Number,
      row : Number,
      sizex : Number,
      sizey : Number,
      width : Number,
      height : Number
    };
    youtubeplayer     : {
      col:  Number,
      row : Number,
      sizex : Number,
      sizey : Number,
      width : Number,
      height : Number
    };
}
